##I created this file to remind us it needs to exist but currently the methods arent accurate 
from space import Space 
class GameBoard:
    spaces = []
    def __init__(self):
        return
    def instantiate GameBoard(self):
        ##Im not sure if this is how we were planning to create the spaces but I was thinking of 
        ##continuing to define all the rooms and spaces like this
        newspace = Space( space_id = 1, occupied = False, adjacentSpaces = [10,2], type = None)
        Library = Room( space_id = 2, occupied = False, adjacentSpaces = [1,3], type = Room)
    

    def get_space(space_id):
        return space_id
